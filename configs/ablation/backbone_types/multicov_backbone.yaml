# TokenExtractor backbone消融实验配置

inherit: "../../base_config.yaml"

# 覆盖基础配置中的backbone设置
model:
  backbone:
    type: "multicov"  # dense, residual, multicov
    in_channels: 1
    out_channels: 128
    kernel_sizes: [3, 5, 7]
