# Scripts

This directory contains auxiliary entry points used during experimentation:

- `run_ablation_experiments.py`: alternate ablation runner
- `run_self_supervised.py`: self-supervised pretraining runner
- `test_model.py`: checkpoint evaluation helper

Run scripts from the repository root so imports and relative paths resolve.
